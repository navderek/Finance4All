rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations in local development
    // TODO: Add proper security rules before production deployment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
